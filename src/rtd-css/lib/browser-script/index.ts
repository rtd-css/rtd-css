import { rtdScript as rtdScript_ } from './lib/rtd-script';
import { uaParserScript as uaParserScript_ } from './lib/ua-parser-script';

export module BrowserScript {
	export const rtdScript = rtdScript_;

	export const uaParserScript = uaParserScript_;
}
